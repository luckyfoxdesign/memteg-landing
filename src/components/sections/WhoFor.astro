---
import { t, type Locale } from '~/i18n';
interface Props { locale: Locale; }
const { locale } = Astro.props;
const dict = t(locale);

const tints = ['bg-[var(--color-lilac)]', 'bg-[var(--color-clay)]', 'bg-[var(--color-mint)]'];
---

<section class="relative">
  <div class="mx-auto w-full max-w-[1200px] px-5 py-24 md:px-10 md:py-32">
    <div class="max-w-2xl" data-reveal>
      <div class="mb-4 font-mono text-[12px] uppercase tracking-wider text-[var(--color-muted)]">
        {dict.whoFor.eyebrow}
      </div>
      <h2 class="text-[clamp(32px,4.5vw,56px)] font-medium leading-[1.02] tracking-[-0.03em]">
        {dict.whoFor.title}
      </h2>
    </div>

    <div class="mt-14 grid gap-5 md:grid-cols-3">
      {
        dict.whoFor.items.map((item, i) => (
          <article class="relative overflow-hidden rounded-[24px] border border-[var(--color-line)] bg-[var(--color-surface)] p-7" data-reveal style={`transition-delay: ${i * 80}ms`}>
            <div class={`mb-6 h-16 w-16 rounded-2xl ${tints[i] ?? tints[0]}`}></div>
            <h3 class="text-[20px] font-medium leading-tight tracking-[-0.015em]">{item.title}</h3>
            <p class="mt-3 text-[14px] leading-relaxed text-[var(--color-muted)]">{item.body}</p>
          </article>
        ))
      }
    </div>
  </div>
</section>
